import { LeaveTypePipe } from './leave-type.pipe';

describe('LeaveTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LeaveTypePipe();
    expect(pipe).toBeTruthy();
  });
});
