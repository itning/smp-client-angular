import { RoomCheckPicSrcPipe } from './room-check-pic-src.pipe';

describe('RoomCheckPicSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomCheckPicSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
